import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

// TODO(Fase 6): configuración de límite máximo por clase/zona + barra de progreso vs. almacenado
export function StorageLimitsScreen() {
  return (
    <ScreenPlaceholder
      title="Límites de almacenamiento"
      description="Cantidad máxima por clase y zona"
    />
  );
}

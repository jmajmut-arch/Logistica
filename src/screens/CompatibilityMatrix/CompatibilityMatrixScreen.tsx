import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

// TODO(Fase 7): grid clase x clase con estado compatible/incompatible, editable por HSE
export function CompatibilityMatrixScreen() {
  return (
    <ScreenPlaceholder
      title="Matriz de compatibilidad"
      description="Compatibilidad entre clases de peligrosidad por zona"
    />
  );
}
